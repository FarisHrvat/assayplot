{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Outbound access to the Notion API, and nothing else. The user supplies their own integration token; it is stored on this machine and sent only to Notion.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://api.notion.com/*" }]
    }
  ]
}
