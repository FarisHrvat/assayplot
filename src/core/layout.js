export function createLayout({rows=1,cols=1,gap=20,width=900,height=560}={}){return{rows,cols,gap,width,height}}
export function panelRects(layout){if(!Number.isInteger(layout.rows)||!Number.isInteger(layout.cols)||layout.rows<1||layout.cols<1)throw new Error('A layout needs positive integer rows and columns.');const panelWidth=(layout.width-layout.gap*(layout.cols+1))/layout.cols,panelHeight=(layout.height-layout.gap*(layout.rows+1))/layout.rows;return Array.from({length:layout.rows*layout.cols},(_,i)=>({x:layout.gap+(i%layout.cols)*(panelWidth+layout.gap),y:layout.gap+Math.floor(i/layout.cols)*(panelHeight+layout.gap),width:panelWidth,height:panelHeight}))}
export function layoutForPanelCount(count){if(count===1)return createLayout({rows:1,cols:1});if(count===2)return createLayout({rows:1,cols:2});if(count===4)return createLayout({rows:2,cols:2});throw new Error('Supported panel counts are 1, 2, or 4.')}
