export function encodeRgbTiff(width,height,rgba){if(!Number.isInteger(width)||!Number.isInteger(height)||width<1||height<1||rgba.length!==width*height*4)throw new Error('TIFF encoding needs RGBA pixels matching the image dimensions.');const pixelBytes=width*height*3,ifdOffset=8,entryCount=10,bitsOffset=ifdOffset+2+entryCount*12+4,dataOffset=bitsOffset+6,buffer=new ArrayBuffer(dataOffset+pixelBytes),view=new DataView(buffer),bytes=new Uint8Array(buffer);bytes[0]=0x49;bytes[1]=0x49;view.setUint16(2,42,true);view.setUint32(4,ifdOffset,true);view.setUint16(ifdOffset,entryCount,true);let entry=ifdOffset+2;const tag=(id,type,count,value)=>{view.setUint16(entry,id,true);view.setUint16(entry+2,type,true);view.setUint32(entry+4,count,true);if(type===3&&count===1)view.setUint16(entry+8,value,true);else view.setUint32(entry+8,value,true);entry+=12};tag(256,4,1,width);tag(257,4,1,height);tag(258,3,3,bitsOffset);tag(259,3,1,1);tag(262,3,1,2);tag(273,4,1,dataOffset);tag(277,3,1,3);tag(278,4,1,height);tag(279,4,1,pixelBytes);tag(284,3,1,1);view.setUint32(entry,0,true);view.setUint16(bitsOffset,8,true);view.setUint16(bitsOffset+2,8,true);view.setUint16(bitsOffset+4,8,true);let output=dataOffset;for(let input=0;input<rgba.length;input+=4){bytes[output++]=rgba[input];bytes[output++]=rgba[input+1];bytes[output++]=rgba[input+2]}return buffer}
