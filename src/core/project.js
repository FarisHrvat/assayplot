export const PROJECT_SCHEMA_VERSION=2;
export function createProject({title='Untitled experiment',data=[],analysis={},analysisHistory=[],figure={},figures=[],roles={}}={}){return{schemaVersion:PROJECT_SCHEMA_VERSION,appVersion:'0.1.0',projectTitle:title,updatedAt:new Date().toISOString(),source:{kind:'local',retained:false},provenance:{engine:'Statista local analysis core',engineVersion:'0.1.0'},data,analysis:{method:'descriptive',variables:[],exclusions:[],multipleComparison:'Holm',options:{},...analysis},analysisHistory,roles,figure,figures}}
export function serializeProject(project){return JSON.stringify({...project,schemaVersion:PROJECT_SCHEMA_VERSION,updatedAt:new Date().toISOString()},null,2)}
export function parseProject(text){let value;try{value=typeof text==='string'?JSON.parse(text):text}catch{throw new Error('This file is not valid JSON.')}if(!value||typeof value!=='object'||!Array.isArray(value.data))throw new Error('This is not a valid Statista project file.');return createProject({title:value.projectTitle||'Untitled experiment',data:value.data.map(row=>Object.fromEntries(Object.entries(row).map(([key,val])=>[key,key==='group'?String(val??''):val===''||val==null?null:(typeof val==='number'?val:(Number.isFinite(Number(val))?Number(val):String(val)))]))),analysis:value.analysis||{},analysisHistory:Array.isArray(value.analysisHistory)?value.analysisHistory:[],roles:value.roles||{},figure:value.figure||{},figures:Array.isArray(value.figures)?value.figures:[]})}
